{
  "name": "homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal browser start page with cross-device sync.",
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
